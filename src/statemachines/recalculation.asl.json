{
  "Comment": "Recalculates the results for each combination of configuration, scenario and product",
  "StartAt": "InitialiseRecalculation",
  "States": {
    "InitialiseRecalculation": {
      "Type": "Task",
      "Resource": "${RecalculationInitiatorFunctionArn}",
      "End": true
    }
  }
}